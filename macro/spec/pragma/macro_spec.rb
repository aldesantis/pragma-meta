# frozen_string_literal: true

RSpec.describe Pragma::Macro do
  it "has a version number" do
    expect(Pragma::Macro::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
